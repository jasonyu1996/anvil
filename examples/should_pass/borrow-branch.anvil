proc top() =
    reg r : logic
    loop {
        if (1'b1) then
            set r := 1'b1
        else (
            let w = !r in
            cycle 1 =>
            dprint "Value = %d" (w)
        )
    }