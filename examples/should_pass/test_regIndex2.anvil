proc test_regIndex2() =
    reg x:logic[2]
    reg y:logic[4]
    loop {
        (
            if 1'b1 then
                set y[!x] := 1'b1
            else 
            dprint "Never Taken" ()
        )=>
        dprint "y: %d" (!y)
    }